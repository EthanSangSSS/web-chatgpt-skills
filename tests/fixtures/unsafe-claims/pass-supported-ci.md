# Fixture: Supported Validation Claim

## User-visible claim

I verified this change against GitHub Actions run `123456789`, where the `validate-catalog` job completed with conclusion `success`.

## Evidence

- Tool output included the workflow run id.
- Tool output included the job conclusion.
- The claim names the exact evidence surface.

## Expected validator result

`PASS`
